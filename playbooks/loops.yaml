- name: Install multiple packages
  hosts: all
  become: true

  tasks:
    - name: Install packages using loop
      apt:
        name: "{{ item }}"
        state: absent
      loop:
        - git
        - wget
        - tree
        - curl
        - maven
